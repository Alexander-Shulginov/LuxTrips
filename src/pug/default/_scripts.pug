- var scriptPath = "./js/";

-
    var scripts = {
        gsap: "gsap.min.js",
        swiper: "swiper.min.js",
        vendor: "vendor.min.js",
        main: "main.min.js",
    }

each script in scripts
    if script
        script(defer, src=`${scriptPath}${script}`)